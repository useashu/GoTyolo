const express = require('express');
const router = express.Router();

// Placeholder - will be implemented in next commits
router.post('/webhook', (req, res) => {
  res.json({ message: 'Payment webhook - coming soon' });
});

module.exports = router;
